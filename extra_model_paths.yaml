autodl_shared:
  base_path: /root/autodl-tmp/shared_models
  
  checkpoints: checkpoints/
  unet: unet/                # 必须添加：对应 Load Diffusion Model 节点
  diffusion_models: unet/    # 增强兼容性
  
  loras: loras/
  vae: vae/
  controlnet: controlnet/
  clip: clip/
  clip_vision: clip_vision/
  upscale_models: upscalers/
  animatediff_models: animatediff/
  ipadapter: ipadapter/